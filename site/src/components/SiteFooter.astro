---
import ContactBlock from './ContactBlock.astro';
import type { NavItem } from './Nav.astro';

interface HoursRow {
  label: string;
  hours: string;
}

interface Props {
  siteName: string;
  navItems: NavItem[];
  addressLines?: string[];
  phone: string;
  phoneHref: string;
  email?: string;
  hours?: HoursRow[];
  /** Plain trust line (e.g. license/insurance) — not a mailing address. */
  trustLine?: string;
  /** Plain service-area line — used in place of a street address when none exists. */
  serviceArea?: string;
}

const { siteName, navItems, addressLines, phone, phoneHref, email, hours, trustLine, serviceArea } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container site-footer-inner">
    <div>
      <p class="site-footer-name">{siteName}</p>
      <ContactBlock addressLines={addressLines} phone={phone} phoneHref={phoneHref} email={email} hours={hours} />
      {serviceArea && <p class="site-footer-note">{serviceArea}</p>}
      {trustLine && <p class="site-footer-note">{trustLine}</p>}
    </div>
    <nav aria-label="Footer">
      <ul class="site-footer-nav">
        {
          navItems.map((item) => (
            <li>
              <a href={item.href}>{item.label}</a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
  <p class="site-footer-legal container">&copy; {year} {siteName}. All rights reserved.</p>
</footer>

<style>
  .site-footer {
    background: var(--c-surface-alt);
    margin-top: var(--sp-6);
    padding-block: var(--sp-5);
  }

  .site-footer-inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--sp-5);
  }

  .site-footer-name {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--fs-3);
    margin: 0 0 var(--sp-2);
  }

  .site-footer-nav {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }

  .site-footer-nav a {
    color: var(--c-ink);
    font-family: var(--font-display);
    text-decoration: none;
    display: inline-block;
    /* WCAG 2.2 SC 2.5.8 (24px min target) — plain text links here measured
       20px tall with no padding (caught in Phase 5 QA's structural audit). */
    padding-block: var(--sp-2);
  }

  .site-footer-nav a:hover {
    color: var(--c-accent);
  }

  .site-footer-note {
    margin: var(--sp-2) 0 0;
    color: var(--c-ink-muted);
    max-width: 32rem;
  }

  .site-footer-legal {
    margin-top: var(--sp-5);
    color: var(--c-ink-muted);
    font-size: var(--fs-1);
  }
</style>
